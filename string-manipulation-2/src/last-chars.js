/* exported lastChars */

// create a variable with empty string
// create a for loop
// set the initial expression as the last character of the string = length - 1
// set the condition expression as greater than or equal to 0
// set the final expression to decrement the counter variable
// concatenate string[i] with variable
// return variable

function lastChars(length, string) {
  var lastChars = '';
  // console.log(string[string.length - 1]);
  // for (var i = ; i >= length; i--) {
  //   lastChars += string[i];
  // }
  return lastChars;
}
